<div class="mb-6">
  <nav class="text-sm text-gray-500 mb-2">
    <%= link_to @network.name, @network, class: "hover:text-gray-700" %> /
  </nav>
  <h1 class="text-2xl font-bold text-gray-900">Add Machine</h1>
  <p class="mt-1 text-sm text-gray-500">Add a machine to deploy ZTLP components on</p>
</div>

<%= render "form", machine: @machine, network: @network %>
