development:
  adapter: async

test:
  adapter: test

production:
  adapter: <%= ENV.fetch("ACTION_CABLE_ADAPTER") { "async" } %>
  url: <%= ENV.fetch("REDIS_URL", "") %>
  channel_prefix: ztlp_bootstrap_production
