# ZTLP-NS (Namespace Server) Configuration
# ==========================================
# All values shown are defaults. Uncomment and modify as needed.
# Environment variables take priority over this file.

# UDP listen port for namespace queries
# Env: ZTLP_NS_PORT
port: 23096

# Maximum records in the store
# Env: ZTLP_NS_MAX_RECORDS
max_records: 100000

# Storage mode: disc (persistent) or ram (volatile, for testing)
# Env: ZTLP_NS_STORAGE_MODE
storage_mode: disc

# Mnesia data directory (leave unset for default location)
# Env: ZTLP_NS_MNESIA_DIR
# mnesia_dir: "/var/lib/ztlp/ns/mnesia"

# Bootstrap discovery URLs
bootstrap_urls:
  - "https://bootstrap.ztlp.org/.well-known/ztlp-relays.json"

# ─── TLS ─────────────────────────────────────────────────────────────
# Optional TLS for inter-component communication.
# NS acts as both TLS server (for queries) and TLS client (cluster peer sync).

# tls:
#   enabled: false
#   cert_file: "/etc/ztlp/ns.crt"
#   key_file: "/etc/ztlp/ns.key"
#   ca_cert_file: "/etc/ztlp/ca.crt"

# ─── Component Authentication ───────────────────────────────────────
# Mutual authentication using Ed25519 challenge-response.

# component_auth:
#   enabled: false
#   identity_key_file: "/etc/ztlp/ns-identity.key"
#   allowed_keys:
#     - "a1b2c3d4e5f6..."  # Relay pubkey
#     - "d4e5f6a1b2c3..."  # Gateway pubkey
#     - "f1e2d3c4b5a6..."  # Other NS peer pubkey
